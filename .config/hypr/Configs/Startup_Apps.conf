exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = wl-paste --type text --watch cliphist store # clipboard
exec-once = wl-paste --type image --watch cliphist store # clipboard
exec-once = wl-paste --watch cliphist store # clipboard
exec-once = awww-daemon # wallaper manager
exec-once = lxqt-policykit-agent # auth client (when use sudo in some app)
